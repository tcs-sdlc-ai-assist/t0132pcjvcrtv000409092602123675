# File-backed SQLite database URL.
DATABASE_URL=sqlite+aiosqlite:///./data/meridian.db
# Replace in deployed environments with a high-entropy secret.
JWT_SECRET=dev-secret-change-in-production
# JWT lifetime in minutes.
JWT_EXPIRE_MINUTES=60
# Seed deterministic demonstration data at startup.
SEED_ON_STARTUP=true
